__pycache__/
*.pyc
build/
dist/
*.spec.bak
build_venv/
# Портативная копия Tesseract, которую build.bat сам готовит перед сборкой —
# бинарник ~50 МБ, пересоздаётся автоматически, в репозитории не нужен.
assets/tesseract/
# Установщик WebView2 (Bootstrapper), который build.bat сам скачивает перед
# сборкой — пересоздаётся автоматически, в репозитории не нужен.
assets/webview2/
# Журнал сборки — пересоздаётся при каждом запуске build.bat.
build_log.txt
